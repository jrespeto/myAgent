# ── One-shot bootstrap (runs first, exits) ──────────────────────────
[program:hermes-dash]
user=hermes
command=/home/hermes/.local/bin/hermes dashboard --host 0.0.0.0 --no-open --insecure
autorestart=false
autostart=false
priority=80