# ── One-shot bootstrap (runs first, exits) ──────────────────────────
[program:prep]
command=/prep.sh
autorestart=false
startsecs=0
exitcodes=0
priority=1
stdout_logfile=/var/log/supervisor/prep.log
stderr_logfile=/var/log/supervisor/prep.log